<!DOCTYPE html>
<html>
<head>
</head>

<body>

    <form action="/upload" method="post" enctype="multipart/form-data" id="addFile">
        <label>Файл</label><br>
        <input type="file" name="filedata" /><br><br>
        <input type="submit" value="Send" />
      </form>
    <!-- <script>
        document.addEventListener('DOMContentLoaded', function () {
            document.getElementById('addFile').addEventListener('submit', function(e) {
                fetch('/addFile').then(r => r.json()).then(r => {
                    alert(r.created)
                })
            })
        })
    </script> -->
</body>
</html>
